package model;

public class Cliente extends Pessoa {
    public Cliente(int pin, String nome, String telefone, String email, String endereco) {
        super(pin, nome, telefone, email, endereco);
    }
}
